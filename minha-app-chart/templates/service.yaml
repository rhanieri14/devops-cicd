apiVersion: v1
kind: Service
metadata:
  name: {{ include "minha-app-chart.fullname" . }}
  labels:
    {{- include "minha-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minha-app-chart.selectorLabels" . | nindent 4 }}
